# dependencies
node_modules/

# next.js build output
.next/
out/

# stale/removed backend scaffold (see CLAUDE.md — apps/web was intentionally
# removed when the workspace flattened to just client/; this leftover
# directory has no package.json and isn't part of the repo)
apps/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo
